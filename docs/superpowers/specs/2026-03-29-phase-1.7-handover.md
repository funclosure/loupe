# Phase 1.7 Handover — Lens Spec & Interaction Refinement

> **For the next session:** Read this file first, then CLAUDE.md, then start working.

## Current State (post Phase 1.6)

File access polished, markdown editing UX fixed. Lens UI redesign shipped in Phase 1.5 — loupe bubbles, drag-to-inspect, warm glass chat, single-expand.

## What Needs Doing

### 1. Lens Spec / Definition Improvements

- Review the LENS.md format — is the frontmatter sufficient? Do lenses need more configuration options (model override, temperature, max tokens, quick actions)?
- Lens validation — better error messages when LENS.md is malformed
- Consider lens categories or tags for organization when many user lenses exist

### 2. Interaction Refinement

Polish based on real usage of the Phase 1.5 lens UI:

- **Drag-to-inspect polish** — snap-back animation smoothness, ghost state during drag, cancel behavior
- **Chat card UX** — scroll behavior, message rendering, streaming smoothness
- **Bubble states** — transitions between idle/thinking/error, visual feedback
- **Auto-focus behavior** — review `use-paragraph-focus.ts` dwell detection, tune the 2-second threshold, consider disabling when a lens is expanded
- **Multiple lenses on same paragraph** — how should this work visually?

### 3. Lens Preset Review

- Are the 5 built-in lenses well-tuned? Review system prompts for quality
- Consider adding 1-2 more presets based on usage patterns

## Out of Scope

- Autonomous lens thinking (Phase 2)
- Lens-to-lens dialogue (Phase 2)
- Diff/suggestion system (Phase 3)
